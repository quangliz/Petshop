# Biến cho docker-compose (Postgres service)
# Copy file này thành .env và điền giá trị THẬT, KHÔNG commit .env
POSTGRES_USER=petshop
POSTGRES_PASSWORD=change_this_strong_password
POSTGRES_DB=petshop_db
