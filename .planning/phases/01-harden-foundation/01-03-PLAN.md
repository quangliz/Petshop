---
phase: 01-harden-foundation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/services/indexing.py
  - frontend/src/lib/types.ts
  - frontend/src/app/(shop)/shop/page.tsx
autonomous: true
requirements: [PERF-04, CODE-04]
must_haves:
  truths:
    - "backend/app/services/indexing.py contains no `except Exception: pass` block — every caught exception is logged via logging.exception()"
    - "frontend/src/lib/types.ts declares only `review_count?: number` (the `reviews_count?` field is removed)"
    - "No frontend file references `reviews_count` — all usages migrated to `review_count`"
    - "Backend type check + frontend build still pass after the field rename"
  artifacts:
    - path: "backend/app/services/indexing.py"
      provides: "Logged error handling for the embedding-delete path (no silent swallow)"
      contains: "logger.exception"
    - path: "frontend/src/lib/types.ts"
      provides: "Single canonical review_count field on Product type"
      contains: "review_count"
  key_links:
    - from: "backend/app/services/indexing.py"
      to: "Python stdlib logging"
      via: "logging.getLogger(__name__).exception(...)"
      pattern: "logger\\.exception"
    - from: "frontend/src/app/(shop)/shop/page.tsx Rating component"
      to: "Product.review_count"
      via: "product.review_count usage"
      pattern: "product\\.review_count"
---

<objective>
Replace the silent `except Exception: pass` block in the indexing service with a logged exception (PERF-04), and unify the dual `reviews_count` / `review_count` field naming in the frontend Product type by keeping only `review_count` (CODE-04). These are independent reliability/code-quality fixes that touch disjoint files from Plans 01, 02, 04 and run safely in Wave 1.

Purpose: Silent exception swallowing in `indexing.py:89-92` hides indexing failures during admin reindex flows — operators never see the error. The dual field naming in `types.ts` causes UI components to read whichever name happens to be populated, leading to "0 reviews" displays even when reviews exist (the API only returns `review_count`).
Output: Logged indexing failures, single canonical review-count field, no broken references.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-harden-foundation/01-CONTEXT.md
@.planning/phases/01-harden-foundation/01-PATTERNS.md
@./CLAUDE.md

@backend/app/services/indexing.py
@frontend/src/lib/types.ts
@frontend/src/app/(shop)/shop/page.tsx

<interfaces>
Python logging pattern (no logger currently exists in indexing.py — use stdlib stdandard):
```python
import logging
logger = logging.getLogger(__name__)
# ...
try:
    await asyncio.to_thread(store.delete, ids=ids)
except Exception:
    logger.exception("Failed to delete embeddings before reindex - proceeding with add")
```
`logger.exception()` automatically includes the traceback at ERROR level — preferred over `logger.error(..., exc_info=True)`.

Frontend Product type (current types.ts lines 36-38):
```typescript
export interface Product {
  // ...
  reviews_count?: number;
  review_count?: number; // Some parts of the API use review_count
  // ...
}
```
Backend reality (per `backend/app/api/routers/products.py`): the API serializes `review_count` only — `reviews_count` is dead.

Single existing frontend consumer of `reviews_count`:
- `frontend/src/app/(shop)/shop/page.tsx:175` — `<Rating value={4.5} count={product.reviews_count || 0} ... />`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace silent exception with logged exception in indexing.py (PERF-04)</name>
  <files>backend/app/services/indexing.py, backend/tests/test_indexing_logging.py</files>
  <read_first>
    - backend/app/services/indexing.py (lines 1-50 for imports + lines 80-100 for the except block)
    - .planning/phases/01-harden-foundation/01-CONTEXT.md (PERF-04, "Claude's Discretion" on logger choice)
    - .planning/phases/01-harden-foundation/01-PATTERNS.md ("backend/app/services/indexing.py" subsection)
  </read_first>
  <behavior>
    - Test 1: When the wrapped delete raises, the except block does NOT re-raise (preserves existing behavior — deletion failure should not abort the reindex)
    - Test 2: When the wrapped delete raises, a record is emitted on the `app.services.indexing` logger at level >= ERROR with traceback information
  </behavior>
  <action>
    In `backend/app/services/indexing.py`:
    1. Add `import logging` to the imports section if not already present (current imports start with `import asyncio` — add `import logging` directly after).
    2. Add a module-level logger right after the imports block (above the first function definition):
       ```python
       logger = logging.getLogger(__name__)
       ```
    3. Locate the silent block (lines 89-92):
       ```python
       try:
           await asyncio.to_thread(store.delete, ids=ids)
       except Exception:  # noqa: BLE001
           pass
       ```
       Replace it with:
       ```python
       try:
           await asyncio.to_thread(store.delete, ids=ids)
       except Exception:
           logger.exception(
               "Failed to delete embeddings before reindex - proceeding with add"
           )
       ```
       The `# noqa: BLE001` comment is REMOVED — the block is no longer a blind catch, ruff will accept it because `logger.exception()` is the documented escape hatch.

    4. Do NOT change the surrounding function signature or its return type. The semantic guarantee is preserved (delete failure is non-fatal, just visible).

    5. Scan the rest of `backend/app/services/indexing.py` for any other `except Exception: pass` patterns. If found, apply the same fix. Per current grep: only line 91 has the issue, but verify before finishing.

    Create `backend/tests/test_indexing_logging.py`:
    - Use `pytest`'s `caplog` fixture set to `logging.ERROR` on the `app.services.indexing` logger.
    - Refactor the inline `try/except/logger.exception` into a small private async helper `_safe_delete_ids(store, ids)` if needed for testability — extracting the 3 lines into a module-level helper makes the test trivial. The helper signature: `async def _safe_delete_ids(store, ids: list[str]) -> None`.
    - Test 1: Pass a mock `store` whose `delete` raises `RuntimeError("boom")`; assert the helper completes without raising.
    - Test 2: Same call, assert `caplog.records` contains a record with `levelno >= logging.ERROR` and `record.exc_info is not None`.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; uv run pytest tests/test_indexing_logging.py -x -v &amp;&amp; ! grep -nE 'except Exception:\s*$' app/services/indexing.py | grep -v '^\s*#'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "import logging" backend/app/services/indexing.py` matches at least once
    - `grep -n "logger = logging.getLogger(__name__)" backend/app/services/indexing.py` matches once
    - `grep -n "logger.exception" backend/app/services/indexing.py` matches at least once
    - `grep -v '^\s*#' backend/app/services/indexing.py | grep -cE 'except Exception:\s*$'` returns 0 (no naked silent excepts on non-comment lines; pipe filters out commented-out lines)
    - `grep -c "# noqa: BLE001" backend/app/services/indexing.py` returns 0 (the noqa marker is gone with the silent block)
    - `uv run pytest tests/test_indexing_logging.py -x` passes 2/2
    - `uv run ruff check backend/app/services/indexing.py` reports no errors
  </acceptance_criteria>
  <done>The reindex pre-delete path logs failures with full traceback instead of swallowing them; tests confirm both no-reraise behavior and logger emission.</done>
</task>

<task type="auto">
  <name>Task 2: Unify reviews_count/review_count to single canonical field (CODE-04)</name>
  <files>frontend/src/lib/types.ts, frontend/src/app/(shop)/shop/page.tsx</files>
  <read_first>
    - frontend/src/lib/types.ts (lines 30-45 — Product interface around the duplicate field)
    - frontend/src/app/(shop)/shop/page.tsx (line 175 — current `product.reviews_count` consumer)
    - .planning/phases/01-harden-foundation/01-CONTEXT.md (CODE-04)
    - .planning/phases/01-harden-foundation/01-PATTERNS.md ("frontend/src/lib/types.ts" subsection)
  </read_first>
  <action>
    1. In `frontend/src/lib/types.ts`, find the two adjacent fields on the `Product` interface:
       ```typescript
       reviews_count?: number;
       review_count?: number; // Some parts of the API use review_count
       ```
       Replace both lines with the single canonical line:
       ```typescript
       review_count?: number;
       ```
       Remove the trailing comment `// Some parts of the API use review_count` — the inconsistency is gone.

    2. Find every frontend consumer of `reviews_count` and migrate it. Authoritative search:
       ```bash
       grep -rn "reviews_count" frontend/src/
       ```
       Current known hit: `frontend/src/app/(shop)/shop/page.tsx:175`:
       ```tsx
       <Rating value={4.5} count={product.reviews_count || 0} size={11} />
       ```
       Change to:
       ```tsx
       <Rating value={4.5} count={product.review_count || 0} size={11} />
       ```
       If grep finds additional references after running it, migrate each to `review_count` in the same task. Do NOT leave any `reviews_count` reference in the frontend tree.

    3. Do NOT touch backend code — the backend already returns `review_count` only.

    4. Run the frontend type check / build to prove no broken references remain:
       ```bash
       cd frontend && npm run lint && npm run build
       ```

    Decision rationale: The backend serializer in `backend/app/api/routers/products.py` only emits `review_count`. The `reviews_count` field has been dead since that serializer was written; removing it eliminates the "Rating shows 0 even though reviews exist" bug in the shop list view (the current page reads the dead field). Per CODE-04 in REQUIREMENTS.md, the unified name must match the API.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; ! grep -rn "reviews_count" src/ &amp;&amp; npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "reviews_count" frontend/src/lib/types.ts` returns 0
    - `grep -c "review_count" frontend/src/lib/types.ts` returns at least 1
    - `grep -rn "reviews_count" frontend/src/` returns no matches (full subtree)
    - `grep -n "product.review_count" frontend/src/app/(shop)/shop/page.tsx` matches at line ~175
    - `cd frontend &amp;&amp; npm run lint` exits 0
    - `cd frontend &amp;&amp; npm run build` exits 0 (next build also runs the type checker)
  </acceptance_criteria>
  <done>Frontend has exactly one review-count field name (`review_count`), no dangling references, and lint+build are green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin reindex action → embedding store | Failures must surface in logs to be diagnosable; silent failures hide DoS-of-search-quality bugs |
| Frontend type contract → backend API response | Mismatched field names cause UI to render incorrect values; not a security boundary but a correctness one |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-12 | Repudiation | Reindex pre-delete failure leaves stale embeddings AND zero log evidence — operators can't tell if AI search results are wrong | mitigate | Replace silent `except: pass` with `logger.exception(...)` so every failure is recorded at ERROR with full traceback |
| T-01-13 | Information Disclosure | None — logger.exception writes to the configured handler chain (stdout/stderr in dev, app log in prod). Traceback content is internal but not user-supplied secrets. | accept | Standard Python logging; no PII flows through this code path |
| T-01-14 | Tampering | UI displays "0 reviews" even when product has reviews due to dead field name — user-visible data integrity issue, not security | mitigate | Remove `reviews_count` from the type and migrate the single consumer; build fails if any consumer is missed |
</threat_model>

<verification>
- `uv run pytest backend/tests/test_indexing_logging.py -v` passes 2/2
- `grep -rn "reviews_count" frontend/src/` returns no matches
- `cd frontend && npm run build` exits 0 with no type errors
- `uv run ruff check backend/app/services/indexing.py` is clean
- Full backend suite `cd backend && uv run pytest` still passes (no regressions)
</verification>

<success_criteria>
- Both requirement IDs (PERF-04, CODE-04) verified by grep + automated test
- No remaining `reviews_count` token anywhere in `frontend/src/`
- Indexing failures are logged with traceback, not silenced
- No backend or frontend regressions
</success_criteria>

<output>
After completion, create `.planning/phases/01-harden-foundation/01-03-SUMMARY.md` documenting: the indexing helper extracted (if any), the unified field name, and any other `reviews_count` references found and migrated beyond the known one in shop/page.tsx.
</output>
