---
phase: 01-harden-foundation
plan: 05
type: execute
wave: 3
depends_on: [04]
files_modified:
  - backend/app/api/routers/admin_knowledge.py
  - backend/app/main.py
  - frontend/src/app/admin/banners/page.tsx
  - frontend/src/app/admin/knowledge/page.tsx
  - frontend/src/app/admin/embeddings/page.tsx
  - frontend/src/components/BannerCarousel.tsx
  - frontend/src/app/(shop)/page.tsx
autonomous: false
requirements: [FEAT-03, FEAT-04]
must_haves:
  truths:
    - "The /admin/banners admin UI page is committed and renders without import errors when navigating in dev mode"
    - "The /admin/knowledge admin UI page is committed and renders the knowledge document list"
    - "The /admin/embeddings admin UI page is committed and renders embedding collection management"
    - "BannerCarousel component is committed and renders on the storefront home page"
    - "All knowledge-base admin endpoints (/admin/knowledge*, /admin/embeddings*) are reachable via main.py mount and protected by require_admin"
    - "Public banner list endpoint (GET /api/v1/banners) returns active banners and is consumed by the storefront carousel"
  artifacts:
    - path: "backend/app/api/routers/admin_knowledge.py"
      provides: "Knowledge document CRUD + embedding collection management endpoints (split out from admin.py)"
      contains: "router = APIRouter"
    - path: "frontend/src/app/admin/banners/page.tsx"
      provides: "Admin banner CRUD UI"
      contains: "export default"
    - path: "frontend/src/app/admin/knowledge/page.tsx"
      provides: "Admin knowledge base CRUD UI"
      contains: "export default"
    - path: "frontend/src/app/admin/embeddings/page.tsx"
      provides: "Admin embedding collection management UI"
      contains: "export default"
    - path: "frontend/src/components/BannerCarousel.tsx"
      provides: "Storefront banner carousel component"
      contains: "export"
  key_links:
    - from: "frontend/src/app/admin/layout.tsx nav"
      to: "/admin/banners, /admin/knowledge, /admin/embeddings pages"
      via: "Next.js App Router file-based routing"
      pattern: "/admin/(banners|knowledge|embeddings)"
    - from: "frontend/src/components/BannerCarousel.tsx"
      to: "GET /api/v1/banners"
      via: "axios api.get('/banners')"
      pattern: "api\\.get.*banners"
    - from: "backend/app/main.py"
      to: "admin_knowledge.router"
      via: "include_router under /api/v1/admin"
      pattern: "include_router\\(admin_knowledge\\.router"
---

<objective>
Commit the untracked banner carousel, knowledge base admin UI, and embeddings admin UI — these are real working features the user has been building locally but never staged. Verify each renders without errors after the admin.py split (Plan 04) lands, and ensure the knowledge/embeddings backend endpoints are properly split out into their own router and mounted in main.py.

This plan addresses FEAT-03 (banner management — frontend + verify backend) and FEAT-04 (knowledge base admin UI — frontend + ensure backend split). Backend banner CRUD already exists in `backend/app/api/routers/banners.py` (untracked) and is already mounted in `main.py:58`; knowledge/embeddings endpoints currently live inside `admin.py` and must end up in their own split file after Plan 04.

Purpose: The features are functionally complete in working memory but not in version control. Phase 1 cannot ship "no missing files" until everything is committed and verified to render.
Output: All untracked feature files committed, knowledge/embeddings endpoints in `admin_knowledge.py`, mounted in main.py, frontend pages render in dev.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-harden-foundation/01-CONTEXT.md
@.planning/phases/01-harden-foundation/01-PATTERNS.md
@./CLAUDE.md

@.planning/phases/01-harden-foundation/01-04-SUMMARY.md

@backend/app/api/routers/banners.py
@backend/app/main.py
@frontend/src/app/admin/layout.tsx
@frontend/src/app/admin/banners/page.tsx
@frontend/src/app/admin/knowledge/page.tsx
@frontend/src/app/admin/embeddings/page.tsx
@frontend/src/components/BannerCarousel.tsx
@frontend/src/app/(shop)/page.tsx

<interfaces>
Untracked file inventory (per `git status` at planning time):
- `backend/app/api/routers/banners.py` — public banner read endpoint (already mounted in main.py:58)
- `backend/app/services/indexing.py` — embedding indexing service (Plan 03 fixed its silent except)
- `backend/alembic/versions/d07c9346d799_add_banners_table.py` — banner table migration
- `backend/alembic/versions/87d3f21ba4b9_add_sold_count_avg_rating_review_count_.py`
- `backend/alembic/versions/e1f2a3b4c5d6_make_order_user_id_nullable.py`
- `frontend/src/app/admin/banners/page.tsx`
- `frontend/src/app/admin/knowledge/page.tsx`
- `frontend/src/app/admin/embeddings/page.tsx`
- `frontend/src/components/BannerCarousel.tsx`
- `imgs/` (asset directory — commit if it contains banner/static assets used by the UI)

Modified files relevant to this plan (already touched in earlier plans):
- `backend/app/main.py` — Plan 01 (CORS/SECRET_KEY), Plan 04 (admin mounts). This plan adds `admin_knowledge` mount.
- `frontend/src/app/admin/layout.tsx` — already references `/admin/banners`, `/admin/knowledge`, `/admin/embeddings` in the nav (lines 14-16)
- `frontend/src/app/(shop)/page.tsx` — modified to render the BannerCarousel

Knowledge / embeddings endpoints currently in `backend/app/api/routers/admin.py` (per grep at planning time):
- Lines 753-820: `/knowledge`, `/knowledge/{doc_id}` (list/get/create/update/delete) — admin_list_knowledge, admin_get_knowledge, admin_create_knowledge, admin_update_knowledge, admin_delete_knowledge
- Lines 820-907: `/embeddings/{collection}`, `/embeddings/{collection}/reindex`, `/embeddings/{collection}/{embedding_id}` — admin_list_embeddings, admin_reindex_collection, admin_delete_embedding (plus `_resolve_collection` helper)

Plan 04's endpoint allocation table did NOT explicitly assign these to a split file. The Plan 04 fallback rule "allocate by resource name in the URL path" leaves `/knowledge` and `/embeddings` with no obvious home — they belong neither to products, orders, users, categories, nor analytics. Plan 05 owns the decision: create a sixth split file `admin_knowledge.py` and mount it.

Banner public endpoint signature (from existing banners.py):
```python
@router.get("/")  # mounted under /api/v1/banners
async def list_banners(db: SessionDep) -> Any:
    # returns list of {id, image_url, title, link_url, sort_order} for is_active=True
```

Frontend axios pattern (from `frontend/src/lib/api.ts`):
```typescript
import { api } from "@/lib/api";
const { data } = await api.get("/banners");
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create admin_knowledge.py split file and mount in main.py</name>
  <files>backend/app/api/routers/admin_knowledge.py, backend/app/main.py, backend/app/api/routers/admin.py</files>
  <read_first>
    - backend/app/api/routers/admin.py (entire file — locate /knowledge and /embeddings endpoint blocks; after Plan 04 runs, this file may already be deleted — handle both cases per branching below)
    - backend/app/main.py (current router mounts after Plan 04's edits)
    - .planning/phases/01-harden-foundation/01-04-SUMMARY.md (Plan 04 must report which file received the knowledge/embeddings endpoints, if any)
    - .planning/phases/01-harden-foundation/01-PATTERNS.md ("Endpoint allocation" reference)
  </read_first>
  <action>
    **Branching based on Plan 04's outcome (read 01-04-SUMMARY.md first):**

    **Case A — Plan 04 left the knowledge/embeddings endpoints in admin.py (it shouldn't have, but verify):**
    - admin.py still exists. Move the endpoints out per the steps below, then delete admin.py.

    **Case B — Plan 04 placed them in admin_categories.py or admin_analytics.py (per its fallback rule):**
    - The endpoints landed in the wrong file. Move them OUT of that file and INTO a new `admin_knowledge.py` per the steps below.

    **Case C — Plan 04 already created admin_knowledge.py:**
    - Skip Task 1 file creation; verify the mount exists in main.py (acceptance criteria still apply to the mount line).

    **Steps for cases A and B:**

    1. Create `backend/app/api/routers/admin_knowledge.py` with this header (matches the analog skeleton in 01-PATTERNS.md):
       ```python
       from typing import Any, Optional
       from fastapi import APIRouter, HTTPException
       from pydantic import BaseModel
       from sqlalchemy import select, desc, func
       import uuid

       from app.api.deps import SessionDep, AdminUser
       from app.models.knowledge import KnowledgeDoc
       from app.services.indexing import reindex_products, reindex_knowledge  # use whatever symbols exist
       from app.services import indexing as indexing_service

       router = APIRouter()
       ```
       Adjust the `from app.services...` import based on the actual symbols `_resolve_collection` and the reindex functions exported from `backend/app/services/indexing.py`.

    2. Move the following endpoints VERBATIM (preserving async signatures per CODE-02 override in Plan 04):
       - `admin_list_knowledge` (GET /knowledge)
       - `admin_get_knowledge` (GET /knowledge/{doc_id})
       - `admin_create_knowledge` (POST /knowledge)
       - `admin_update_knowledge` (PUT /knowledge/{doc_id})
       - `admin_delete_knowledge` (DELETE /knowledge/{doc_id})
       - `admin_list_embeddings` (GET /embeddings/{collection})
       - `admin_reindex_collection` (POST /embeddings/{collection}/reindex)
       - `admin_delete_embedding` (DELETE /embeddings/{collection}/{embedding_id})

    3. Move the supporting Pydantic schemas `KnowledgeCreate`, `KnowledgeUpdate` (admin.py lines 724-737) and the helper functions `_knowledge_dict` (line 738) and `_resolve_collection` (line 820) into `admin_knowledge.py`.

    4. Replace authz pattern at every endpoint per Plan 04's rule:
       - Old: `async def admin_list_knowledge(db: SessionDep, current_user: CurrentUser, ...)` with body `require_admin(current_user)`
       - New: `async def admin_list_knowledge(db: SessionDep, admin: AdminUser, ...)` and remove the imperative `require_admin(...)` line. References to `current_user` in the body become `admin`.

    5. In `backend/app/main.py`:
       - Update the import line `from app.api.routers import admin_products, admin_orders, admin_users, admin_categories, admin_analytics` to also include `admin_knowledge`.
       - Add the mount line right after the other admin mounts:
         ```python
         app.include_router(admin_knowledge.router, prefix=f"{settings.API_V1_STR}/admin", tags=["admin"])
         ```

    6. If endpoints existed in `admin_categories.py` or `admin_analytics.py` from Plan 04 (Case B), DELETE them from those files after moving — verify with `grep -n "/knowledge\|/embeddings" backend/app/api/routers/admin_categories.py` returning 0 matches.

    7. Smoke-test the import:
       ```bash
       cd backend && uv run python -c "from app.main import app; routes = [r.path for r in app.routes]; \
       assert '/api/v1/admin/knowledge' in routes; assert any('/api/v1/admin/embeddings' in p for p in routes); \
       print('admin_knowledge mounted:', sum(1 for p in routes if '/admin/knowledge' in p or '/admin/embeddings' in p), 'routes')"
       ```
  </action>
  <verify>
    <automated>cd backend &amp;&amp; uv run python -c "from app.main import app; paths = [r.path for r in app.routes]; \
assert any('/api/v1/admin/knowledge' in p for p in paths), 'knowledge routes missing'; \
assert any('/api/v1/admin/embeddings' in p for p in paths), 'embeddings routes missing'; \
print('mounted:', [p for p in paths if '/admin/knowledge' in p or '/admin/embeddings' in p])" &amp;&amp; uv run pytest tests/test_admin_authz.py -x -v &amp;&amp; uv run ruff check app/api/routers/admin_knowledge.py app/main.py</automated>
  </verify>
  <acceptance_criteria>
    - File `backend/app/api/routers/admin_knowledge.py` exists and is non-empty
    - `grep -c "router = APIRouter" backend/app/api/routers/admin_knowledge.py` returns exactly 1
    - `grep -c "@router\." backend/app/api/routers/admin_knowledge.py` returns at least 8 (5 knowledge + 3 embeddings endpoints)
    - `grep -c "AdminUser" backend/app/api/routers/admin_knowledge.py` returns at least 8 (one per endpoint signature)
    - `grep -c "require_admin(current_user)" backend/app/api/routers/admin_knowledge.py` returns 0 (imperative pattern removed)
    - `grep -c "await db\." backend/app/api/routers/admin_knowledge.py` returns at least 5 (async preserved per CODE-02 override)
    - `grep -n "admin_knowledge" backend/app/main.py` matches at least 2 times (import + include_router)
    - `grep -c "include_router(admin_knowledge.router" backend/app/main.py` returns exactly 1
    - `grep -rn "/knowledge\|/embeddings" backend/app/api/routers/admin_categories.py backend/app/api/routers/admin_analytics.py 2>/dev/null` returns 0 (endpoints not duplicated)
    - The verify command exits 0 — both knowledge and embeddings paths resolve in app.routes
    - `uv run ruff check backend/app/api/routers/admin_knowledge.py backend/app/main.py` reports no errors
  </acceptance_criteria>
  <done>admin_knowledge.py owns all /admin/knowledge/* and /admin/embeddings/* endpoints; main.py mounts it; no leftovers in other split files; smoke test confirms routes resolve.</done>
</task>

<task type="auto">
  <name>Task 2: Verify untracked frontend feature files build and lint clean</name>
  <files>frontend/src/app/admin/banners/page.tsx, frontend/src/app/admin/knowledge/page.tsx, frontend/src/app/admin/embeddings/page.tsx, frontend/src/components/BannerCarousel.tsx, frontend/src/app/(shop)/page.tsx</files>
  <read_first>
    - frontend/src/app/admin/banners/page.tsx (full file — verify imports resolve)
    - frontend/src/app/admin/knowledge/page.tsx
    - frontend/src/app/admin/embeddings/page.tsx
    - frontend/src/components/BannerCarousel.tsx
    - frontend/src/app/(shop)/page.tsx (verify BannerCarousel is imported and rendered)
    - frontend/src/app/admin/layout.tsx (lines 14-16 — confirm nav links exist for all three admin pages)
    - frontend/src/lib/api.ts (the shared axios instance — confirm baseURL and bearer attachment)
  </read_first>
  <action>
    The frontend feature files are already on disk but untracked. They need verification, NOT rewriting:

    1. Read each of the five files end-to-end. For each, verify:
       - The file has a default export (Next.js App Router requires it for route pages; component files just need an `export`)
       - All imports resolve (no references to missing modules)
       - All API calls use the shared `api` instance from `@/lib/api` (NOT raw `fetch` to `localhost:8000` and NOT a fresh `axios.create`)
       - The endpoint paths called match the backend routes:
         - banners admin page → `/admin/banners` (Plan 04's admin_categories.py owns these per its allocation; verify by grep against admin_categories.py routes)
         - knowledge admin page → `/admin/knowledge`, `/admin/knowledge/{id}` (admin_knowledge.py from Task 1)
         - embeddings admin page → `/admin/embeddings/{collection}`, `/admin/embeddings/{collection}/reindex` (admin_knowledge.py from Task 1)
         - BannerCarousel → `/banners` (public banners.py router)

    2. If any of the above checks fails, fix it MINIMALLY — replace raw fetch with `api.*`, correct an endpoint path, etc. Do NOT redesign the components.

    3. Verify `frontend/src/app/(shop)/page.tsx` imports and renders `BannerCarousel`. If not, add the import and the render in the appropriate location near the top of the page (above the product grid). Use:
       ```tsx
       import { BannerCarousel } from "@/components/BannerCarousel";
       // inside the page component, near the top:
       <BannerCarousel />
       ```
       Adjust import name to match the actual export shape (default vs named) of BannerCarousel.tsx.

    4. Verify `frontend/src/app/admin/layout.tsx` already has all three nav entries (banners, knowledge, embeddings) — per grep at planning time it does. If any entry is missing, add it following the existing pattern (icon + label + href).

    5. Run lint and build to prove everything compiles:
       ```bash
       cd frontend && npm run lint && npm run build
       ```
       Both must exit 0. If build fails, the most likely cause is a type mismatch between a frontend interface and a backend response — fix the frontend type or the API call shape, never the backend response.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npm run lint &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - All 5 files exist and are non-empty: `for f in src/app/admin/banners/page.tsx src/app/admin/knowledge/page.tsx src/app/admin/embeddings/page.tsx src/components/BannerCarousel.tsx src/app/\(shop\)/page.tsx; do test -s "frontend/$f" || exit 1; done`
    - `grep -l "export default" frontend/src/app/admin/banners/page.tsx frontend/src/app/admin/knowledge/page.tsx frontend/src/app/admin/embeddings/page.tsx` lists all 3 (page files have default export)
    - `grep -c "from \"@/lib/api\"\|from '@/lib/api'" frontend/src/app/admin/banners/page.tsx frontend/src/app/admin/knowledge/page.tsx frontend/src/app/admin/embeddings/page.tsx frontend/src/components/BannerCarousel.tsx` is at least 4 (one import per file that calls the API)
    - `grep -E "fetch\(['\"]http://localhost:8000" frontend/src/app/admin/banners/page.tsx frontend/src/app/admin/knowledge/page.tsx frontend/src/app/admin/embeddings/page.tsx frontend/src/components/BannerCarousel.tsx` returns no matches (no raw fetches bypassing the shared client)
    - `grep -n "BannerCarousel" frontend/src/app/\(shop\)/page.tsx` matches at least 2 times (import + render)
    - `cd frontend &amp;&amp; npm run lint` exits 0
    - `cd frontend &amp;&amp; npm run build` exits 0
  </acceptance_criteria>
  <done>All four untracked frontend files compile and lint clean; storefront page renders the carousel; admin layout has all three nav entries.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Manually verify all three admin pages and storefront banner render</name>
  <what-built>
    Untracked feature files committed and verified:
    - Backend: admin_knowledge.py created and mounted; banners.py already public-mounted
    - Frontend admin: /admin/banners, /admin/knowledge, /admin/embeddings pages
    - Frontend storefront: BannerCarousel rendered on the home page
  </what-built>
  <how-to-verify>
    1. Start the backend: `cd backend && uv run uvicorn app.main:app --reload`. Confirm it starts without RuntimeError (Plan 01 SECRET_KEY guard) and serves http://localhost:8000/health returning `{"status":"ok"}`.
    2. Start the frontend: `cd frontend && npm run dev`. Confirm it boots without errors at http://localhost:3000.
    3. Log in as an admin user. If no admin user exists, promote one via `cd backend && uv run python -c "import asyncio; from app.database import AsyncSessionLocal; from app.models.user import User, RoleEnum; from sqlalchemy import update; async def f(): async with AsyncSessionLocal() as s: await s.execute(update(User).where(User.email=='YOUR_EMAIL').values(role=RoleEnum.admin)); await s.commit(); asyncio.run(f())"` (substitute your test email).
    4. Visit http://localhost:3000/admin/banners. Confirm: (a) page loads without console error, (b) banner list renders (or shows an empty state), (c) "create banner" form is functional (try uploading or filling fields — actual save is optional).
    5. Visit http://localhost:3000/admin/knowledge. Confirm: (a) loads, (b) shows knowledge document list with create/edit affordances.
    6. Visit http://localhost:3000/admin/embeddings. Confirm: (a) loads, (b) shows at least one embedding collection (e.g. `products`) with reindex button.
    7. Visit http://localhost:3000/ (storefront home). Confirm BannerCarousel renders at the top of the page (or shows graceful empty state if no active banners exist).
    8. Open the browser DevTools Network tab while navigating. Confirm:
       - Calls to `/api/v1/admin/knowledge` and `/api/v1/admin/embeddings/...` return 200 with `Authorization: Bearer ...` header (NOT 401/403)
       - Calls to `/api/v1/banners` from the storefront return 200
    9. Log out (or open an incognito window without a token). Visit http://localhost:3000/admin/banners directly. Confirm the underlying API call to `/api/v1/admin/banners` returns 401 in DevTools (the page itself may redirect or render an empty state — both acceptable; the backend 401 is the SEC-03 proof).
  </how-to-verify>
  <resume-signal>Type "approved" if all six render checks pass and step 9 returns 401, or describe any failure (page-load error, missing data, wrong status code).</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Stage and commit all Phase 1 untracked + modified files</name>
  <files>backend/app/api/routers/admin_knowledge.py, backend/app/api/routers/banners.py, backend/app/services/indexing.py, frontend/src/app/admin/banners/page.tsx, frontend/src/app/admin/knowledge/page.tsx, frontend/src/app/admin/embeddings/page.tsx, frontend/src/components/BannerCarousel.tsx, frontend/src/app/(shop)/page.tsx, backend/alembic/versions/87d3f21ba4b9_add_sold_count_avg_rating_review_count_.py, backend/alembic/versions/d07c9346d799_add_banners_table.py, backend/alembic/versions/e1f2a3b4c5d6_make_order_user_id_nullable.py</files>
  <read_first>
    - Output of `git status` (current state of staging area after Plans 01-04 ran)
    - .planning/phases/01-harden-foundation/01-04-SUMMARY.md (confirms admin.py was deleted)
  </read_first>
  <action>
    Run `git status` first to see the actual staging state — Plans 01, 02, 03, 04 may have already staged some of their own files. The goal of this task is to commit the FEAT-03/FEAT-04 untracked files plus the Alembic migrations that were created for prior phases of work.

    Stage the FEAT-03/FEAT-04 files explicitly (do NOT use `git add -A` — per project safety protocol):
    ```bash
    cd /home/quang/Documents/DATN
    git add backend/app/api/routers/admin_knowledge.py
    git add backend/app/api/routers/banners.py
    git add backend/app/services/indexing.py
    git add backend/alembic/versions/87d3f21ba4b9_add_sold_count_avg_rating_review_count_.py
    git add backend/alembic/versions/d07c9346d799_add_banners_table.py
    git add backend/alembic/versions/e1f2a3b4c5d6_make_order_user_id_nullable.py
    git add frontend/src/app/admin/banners/
    git add frontend/src/app/admin/knowledge/
    git add frontend/src/app/admin/embeddings/
    git add frontend/src/components/BannerCarousel.tsx
    ```

    For files that may have been modified earlier in Phase 1 but not yet committed by their owning plan (verify via `git status`):
    - `backend/app/main.py` — has admin mount changes from Plan 04 + admin_knowledge mount from this plan's Task 1
    - `frontend/src/app/(shop)/page.tsx` — has BannerCarousel render from Task 2

    For each modified file, decide: if its owning plan (per the per-plan SUMMARY files) already committed it, skip; if not, `git add` and include in this commit.

    Inspect any file under `imgs/` (untracked per git status). If it contains static assets actually referenced by the committed UI files (e.g. seeded banner images), add those specific files. Otherwise leave `imgs/` untracked.

    Do NOT stage:
    - `.env` or any file matching env patterns
    - Any file containing `SECRET_KEY=`, `OPENAI_API_KEY=`, or VNPay secrets — verify by `git diff --cached | grep -iE "secret|password|api_key" | grep -v "ALLOWED_ORIGINS\|REDIS_URL\|class.*Settings"`

    Commit with HEREDOC:
    ```bash
    git commit -m "$(cat <<'EOF'
    feat(01-harden-foundation): commit banner carousel, knowledge admin UI, embeddings admin UI

    - Adds untracked banner backend router (already mounted) and frontend admin page
    - Adds untracked knowledge base admin UI + splits backend endpoints into admin_knowledge.py
    - Adds untracked embeddings admin UI for collection management
    - Adds untracked alembic migrations for banners, sold_count/review_count, nullable order user_id
    - Renders BannerCarousel on storefront home page
    - Mounts admin_knowledge.router under /api/v1/admin

    Closes FEAT-03 (banner management committed and functional) and FEAT-04 (knowledge base admin UI committed and functional).

    Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>
    EOF
    )"
    ```

    After commit, run `git status` and confirm the working tree is clean of FEAT-03/FEAT-04 files.
  </action>
  <verify>
    <automated>cd /home/quang/Documents/DATN &amp;&amp; git log -1 --stat | grep -cE "admin_knowledge\.py|banners\.py|indexing\.py|admin/banners/page|admin/knowledge/page|admin/embeddings/page|BannerCarousel"</automated>
  </verify>
  <acceptance_criteria>
    - `git log -1 --stat` shows all FEAT-03/FEAT-04 file paths in the commit
    - `git status --porcelain | grep -E "^\?\?.*(admin_knowledge|banners\.py$|admin/banners/|admin/knowledge/|admin/embeddings/|BannerCarousel|d07c9346d799|87d3f21ba4b9|e1f2a3b4c5d6)"` returns 0 (none of these files remain untracked)
    - `git diff --cached --name-only` returns empty (nothing left staged after the commit)
    - `git log --oneline -1` shows a commit with prefix `feat(01-harden-foundation):`
    - The verify command returns at least 6 (most FEAT-03/FEAT-04 path tokens appear in the commit)
    - No file matching `\.env$` or containing the substring `SECRET_KEY=<actual_value>` was added to the commit
  </acceptance_criteria>
  <done>All untracked FEAT-03/FEAT-04 files plus their alembic migrations are committed; working tree clean for the relevant paths; commit message references the requirement IDs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin browser → /admin/knowledge endpoints | Knowledge documents may contain prompt-injection content; backend stores raw, render must be safe |
| Admin browser → /admin/embeddings/reindex | Reindex triggers expensive OpenAI calls; must be admin-only and not exposed to non-admin tokens |
| Public browser → /api/v1/banners | Banner image_url may be user-supplied (admin-uploaded); the URL is rendered in <img src=>; same-origin policy applies but not a security boundary for the carousel itself |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-15 | Elevation of Privilege | A non-admin JWT could trigger expensive embedding reindex on /admin/embeddings/{collection}/reindex | mitigate | Endpoints in admin_knowledge.py use the AdminUser dependency from deps.py (Plan 04 / SEC-03) — non-admin returns 403 before reindex runs |
| T-01-16 | Denial of Service | An admin can trigger /admin/embeddings/reindex repeatedly, costing OpenAI credits and blocking the event loop briefly | accept | Out of Phase 1 scope; rate-limit on reindex deferred to Phase 2 (cost concern, not security). Document in summary. |
| T-01-17 | Tampering | Knowledge doc content stored in DB and later embedded — malicious admin could inject prompt-engineering text | accept | Trust boundary is admin-side; we trust admins. RAG retrieval in Phase 2 will additionally use system prompts that constrain the agent. |
| T-01-18 | Information Disclosure | Banner image_url may point to private Cloudinary asset that leaks via public /banners endpoint | accept | Banners are intended to be public marketing material; admin chooses what to upload. |
| T-01-19 | Spoofing | The committed admin frontend pages rely on JWT bearer token from localStorage — XSS could exfiltrate | mitigate | Defense in depth: the backend SEC-03 enforcement (Plan 04) means a stolen non-admin token still cannot hit admin endpoints. XSS hardening is broader than Phase 1 scope. |
</threat_model>

<verification>
- All six router paths under /api/v1/admin/knowledge and /api/v1/admin/embeddings resolve when the app boots
- All four untracked frontend files compile (`cd frontend && npm run build` exits 0)
- Admin pages render without console errors in dev (human verification, Task 3)
- Storefront home page shows the BannerCarousel
- `git status --porcelain` shows no remaining untracked Phase 1 feature files after Task 4 commit
- Full backend pytest suite passes (`cd backend && uv run pytest`)
</verification>

<success_criteria>
- FEAT-03: Banner management is fully committed (backend router + admin UI + storefront carousel) and the storefront banner renders
- FEAT-04: Knowledge base admin UI is committed and accessible at /admin/knowledge; embeddings UI at /admin/embeddings; backend endpoints split into admin_knowledge.py
- The single commit produced by Task 4 references both requirement IDs
- `git status` after this plan shows no untracked feature files (only optional `imgs/` artifact directory if not referenced)
</success_criteria>

<output>
After completion, create `.planning/phases/01-harden-foundation/01-05-SUMMARY.md` documenting:
- Whether admin_knowledge.py was created fresh, moved from another split file, or already existed
- The list of endpoints landed in admin_knowledge.py (with HTTP method + path)
- Each frontend file's API endpoint dependencies (which backend route each page calls)
- The commit SHA produced by Task 4
- Any deferred items found during verification (e.g. reindex rate-limit accepted as deferred)
</output>
