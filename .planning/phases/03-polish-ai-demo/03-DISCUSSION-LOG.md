# Phase 3: Polish AI Demo — Discussion Log

**Date:** 2026-04-30
**Phase:** 3 — Polish AI Demo
**Areas discussed:** 2 of 4 identified (Areas 1 and 3 selected by user)

---

## Area 1: Cart Tool Design

**Gray area:** The LangGraph agent needs `add_to_cart` and `view_cart` tools. The core questions were how to mutate state, how to pass user identity, how to signal the frontend, and how to identify the product from natural language.

| # | Question | Options Presented | Selection | Notes |
|---|---|---|---|---|
| 1 | How should `add_to_cart` mutate state? | A) Direct DB write, B) Internal HTTP call, C) Emit structured event | **A** | Same closure pattern as `search_products_tool` |
| 2 | How does the tool get `user_id`? | A) Extend `_build_tools(db, user_id)`, B) Thread via agent state, C) Pass full `current_user` | **A** | One-line call-site change in `chat.py` |
| 3 | How does the frontend know cart was updated? | A) Emit `cart_updated` SSE event, B) Frontend polls on `done`, C) LLM confirmation only | **A** | Mirrors existing `products` SSE event |
| 4 | How does tool identify product from natural language? | A) Slug (string), B) product_id (UUID), C) Both | **A** | LLM already has slugs from `search_products_tool` results |

**Outcome:** Direct DB write, `_build_tools(db, user_id)`, `cart_updated` SSE event, slug-based product lookup.

---

## Area 2: Knowledge Base Citation Format (not discussed — agent discretion)

User did not select this area. Agent decision: follow existing `<product>` tag convention; add inline citation text alongside knowledge base content (no new tag or event type needed).

---

## Area 3: Guest Order Lookup

**Gray area:** FEAT-05 requires guests to find their order by email + order code. No backend endpoint or frontend page exists yet.

| # | Question | Options Presented | Selection | Notes |
|---|---|---|---|---|
| 1 | Where should the lookup page live? | A) `/tra-cuu-don-hang`, B) Extend `/orders` with tab, C) `/orders/lookup` | **A** | Vietnamese slug, no auth, linkable from confirmation |
| 2 | What does the backend endpoint look like? | A) `GET /orders/guest-lookup?email=&order_code=`, B) `POST /orders/guest-lookup`, C) `GET /orders/by-code/{order_code}` | **A** | Public route, no JWT, query params |
| 3 | What level of detail on result page? | A) Full order detail, B) Status-only, C) Full with masked address | **A** | Reuse existing `/orders/[id]` component |
| 4 | Where is lookup page linked from? | A) Confirmation page only, B) Footer + confirmation, C) Confirmation + email reminder | **A** | Natural moment — guest has order code right there |

**Outcome:** `/tra-cuu-don-hang` page, `GET /orders/guest-lookup` public endpoint, full detail reusing order detail component, linked from checkout confirmation.

---

## Area 4: Product Link Format in Chat (not discussed — agent discretion)

User did not select this area. Agent decision: `<product>slug</product>` tag (already implemented, renders as cards) is sufficient for AI-10. No additional link format needed.

---

## Deferred Ideas

None captured during discussion.

---

*Generated by gsd-discuss-phase*
