---
phase: 3
slug: polish-ai-demo
status: draft
shadcn_initialized: true
preset: base-nova
created: 2026-04-30
---

# Phase 3 — UI Design Contract: Polish AI Demo

> Visual and interaction contract for Phase 3 frontend work. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | shadcn/ui |
| Preset | base-nova |
| Style | base-nova (from `components.json`) |
| Component library | Radix UI (via shadcn base-nova) |
| Icon library | lucide-react |
| Font | System default (inherited from existing globals) |
| Base color | neutral |
| CSS variables | true |

Source: `frontend/components.json` (detected, confirmed).

---

## Spacing Scale

Declared values (multiples of 4):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, badge padding, inline gaps |
| sm | 8px | Compact element spacing, chat bubble padding internal |
| md | 16px | Default element spacing, form field gaps |
| lg | 24px | Section padding, card internal padding |
| xl | 32px | Layout gaps between major sections |
| 2xl | 48px | Major section breaks |
| 3xl | 64px | Page-level spacing (unused in chat/modal contexts) |

Exceptions:
- Chat input touch target minimum: 44px height (accessibility)
- Cart badge on chat widget FAB: 20px diameter circle overlay

Source: 8-point scale default; exceptions from AI chat widget context (CONTEXT.md D-03).

---

## Typography

| Role | Size | Weight | Line Height |
|------|------|--------|-------------|
| Body | 16px | 400 (regular) | 1.5 |
| Label / Small | 14px | 600 (semibold) | 1.4 |
| Heading | 20px | 600 (semibold) | 1.2 |
| Display | 28px | 600 (semibold) | 1.15 |

Notes:
- Vietnamese text renders correctly at 16px/1.5 — do not reduce below 14px for any body copy.
- Chat message bubbles: 16px / weight 400 / line-height 1.6 (looser line-height for conversational readability; same size as body).
- Error and empty-state copy: 14px / weight 400.

Source: globals.css `.btn` pattern (14px/600) + body default (16px/400); chat line-height from CONTEXT.md `<specifics>`.

---

## Color

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `var(--neutral-50)` = `#faf7f2` | Page background, form backgrounds |
| Secondary (30%) | `white` / `var(--neutral-0)` + `var(--neutral-100)` | Cards, chat bubbles (assistant), order detail panels, input surfaces |
| Accent (10%) | `var(--primary-500)` = `oklch(0.68 0.19 50)` warm orange | Primary CTA buttons, active states, cart badge |
| AI Accent | `var(--teal-600)` = `oklch(0.54 0.12 192)` teal | AI-sourced content only: chat FAB, AI badge on product cards, `<source>` citation chips, streaming indicator dot |
| Destructive | `var(--danger)` = `oklch(0.58 0.19 25)` | Destructive actions only (none in this phase — no destructive flows) |

Accent (orange) reserved for:
- "Thêm vào giỏ" (Add to cart) primary button
- "Tra cứu" (Lookup) submit button on guest order page
- Cart item count badge on chat FAB
- Active/selected nav state

AI accent (teal) reserved for:
- Chat widget FAB button background
- "AI" badge
- Source citation chips rendered inline in chat responses
- Streaming cursor / loading indicator inside chat

Source: `globals.css` color palette (detected); teal-as-AI convention from existing `.badge-ai` class + CONTEXT.md AI-11 (knowledge base citations).

---

## Visual Hierarchy & Focal Points

| Screen / State | Primary Focal Point | Rationale |
|----------------|--------------------|-----------| 
| `/tra-cuu-don-hang` (initial) | Two-field form (email + order code) centered in the content column, with the orange "Tra cuu" button immediately below | The entire page exists to accept lookup input; nothing else competes |
| `/tra-cuu-don-hang` (result) | Order detail panel rendered inline below the form | Form remains visible but visually subordinate (reduced opacity or separator line); order detail is the new focal point |
| ChatWidget open state | Message input bar at the bottom of the widget | Fixed position, accent-bordered on focus, 44px touch target — draws the eye after reading the message history above |
| ChatWidget message list | Most recent AI message (bottom of scroll area) | Auto-scroll on new message ensures latest content is always in view |

Source: checker flag (Dimension 2) — no focal point declared in prior draft; declared here per interaction states defined in CONTEXT.md D-03, D-05.

---

## Component Inventory

New or modified components this phase:

| Component | Type | Location | Notes |
|-----------|------|----------|-------|
| `CartUpdatedToast` | New | `src/components/chat/` | Shown when AI adds to cart via `cart_updated` SSE event. Displays product name + new cart count. Auto-dismisses after 3s. |
| `SourceCitation` | New | `src/components/chat/` | Inline chip inside chat message: shows knowledge base document title. Teal background (`teal-50`), teal text (`teal-700`), pill border-radius. |
| `GuestOrderLookup` | New page | `src/app/(shop)/tra-cuu-don-hang/page.tsx` | Two-field form (email + order code) + inline result panel. No auth. |
| `ChatWidget` | Modified | `src/components/chat/ChatWidget.tsx` | Add `cart_updated` SSE event handler; refresh cart count badge without full page reload. |
| `OrderDetail` | Reused | `src/app/(shop)/orders/[id]/` | Reused verbatim on guest lookup result — no visual changes. |

Source: CONTEXT.md D-03, D-05, D-07, AI-11; `<specifics>` section.

---

## Interaction States

### Chat: Cart Tool Flow

| State | Visual |
|-------|--------|
| User sends add-to-cart message | Normal chat bubble, streaming response starts |
| LLM calls `add_to_cart` tool | No visual change mid-stream |
| `cart_updated` SSE event received | `CartUpdatedToast` slides in from bottom-right: "Da them [Product Name] vao gio hang" + cart count badge updates |
| Tool failure | AI responds in chat with Vietnamese error copy (no toast) |

### Chat: View Cart Tool Flow

| State | Visual |
|-------|--------|
| User asks about cart | LLM responds with Vietnamese text list (name, qty, price) inline in chat bubble — no special component |
| Empty cart | AI responds: "Gio hang cua ban dang trong." |

### Guest Order Lookup Page

| State | Visual |
|-------|--------|
| Initial | Two inputs (email, order code) + "Tra cuu" primary button. Both fields required. Focal point: the form. |
| Loading | Button shows spinner, disabled. Inputs disabled. |
| Success | Order detail panel renders inline below the form. Form remains visible (allow re-lookup). Focal point shifts to order detail panel. |
| Not found | Inline error below form (no toast): Vietnamese error copy (see Copywriting Contract). |
| Network error | Inline error: Vietnamese generic error copy (see Copywriting Contract). |

---

## Copywriting Contract

All copy in Vietnamese unless otherwise noted.

| Element | Copy |
|---------|------|
| Primary CTA — add to cart (AI) | Automatically triggered by AI — no user-facing button label |
| Primary CTA — guest lookup | "Tra cuu don hang" |
| Primary CTA — checkout link from confirmation | "Tra cuu don hang cua ban" (link text on confirmation page) |
| Cart updated toast heading | "Da them vao gio hang" |
| Cart updated toast body | "{Ten san pham} x{so luong} — Gio hang: {N} san pham" |
| Guest lookup empty/not found | "Khong tim thay don hang voi thong tin da nhap. Vui long kiem tra lai email va ma don hang." |
| Guest lookup network error | "Co loi xay ra. Vui long thu lai sau." |
| Empty cart (view_cart tool response) | AI generates: "Gio hang cua ban dang trong. Ban co muon xem cac san pham?" |
| Knowledge base citation label | "Nguon:" followed by document title in teal chip |
| Guest lookup page heading | "Tra cuu don hang" |
| Guest lookup page subheading | "Nhap email va ma don hang de xem trang thai don hang cua ban." |
| Email field label | "Email dat hang" |
| Order code field label | "Ma don hang" |
| Email field placeholder | "example@email.com" |
| Order code field placeholder | "VD: PET-20240101-XXXX" |
| Destructive confirmation | None — no destructive actions in this phase |

Source: CONTEXT.md `<specifics>` (not-found copy), D-08 (confirmation page link), D-05 (page location); defaults applied for labels.

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | Button, Input, Card, Badge, Toast/Sonner, Spinner | not required |
| Third-party | none | not applicable |

Source: `components.json` `registries: {}` — no third-party registries declared.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
