---
phase: 5
plan: 1
title: "Toast system + Spinner component + Empty states"
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/ui/sonner.tsx
  - frontend/src/components/ui/spinner.tsx
  - frontend/src/components/ui/empty-state.tsx
  - frontend/src/app/layout.tsx
  - frontend/src/app/(shop)/shop/page.tsx
  - frontend/src/app/(shop)/products/[slug]/page.tsx
  - frontend/src/app/(shop)/cart/page.tsx
  - frontend/src/app/(shop)/orders/page.tsx
autonomous: true
requirements:
  - UX-05
  - UX-08
  - UX-09
  - UX-10
  - UX-11
---

<objective>
Install the sonner toast library via shadcn, create a reusable Spinner component, create a reusable EmptyState component, and wire toast notifications + spinners into the shop listing, product detail, cart, and orders pages. Replace all `alert()` calls with `toast.success()` / `toast.error()`. Add proper empty states for cart, orders, and search-no-results.
</objective>

<tasks>

<task id="1" type="execute">
<title>Install sonner via shadcn and add Toaster to app layout</title>
<files>
  - frontend/src/components/ui/sonner.tsx (CREATE)
  - frontend/src/app/layout.tsx (MODIFY)
</files>
<read_first>
  - frontend/src/app/layout.tsx
  - frontend/src/app/globals.css
</read_first>
<action>
1. Run `npx shadcn@latest add sonner` in the `frontend/` directory to install the sonner toast component.
2. If the CLI creates `frontend/src/components/ui/sonner.tsx`, review it. It should export a `<Toaster />` component.
3. Open `frontend/src/app/layout.tsx` and:
   - Add `import { Toaster } from '@/components/ui/sonner';`
   - Add `<Toaster richColors position="bottom-right" toastOptions={{ style: { borderRadius: '12px', fontWeight: 600, fontSize: '13px' } }} />` just before the closing `</body>` tag (inside the `QueryProvider` if present, so toasts can interact with query state).

The Toaster should use the `richColors` prop so success/error toasts have distinct green/red backgrounds.
</action>
<acceptance_criteria>
  - File `frontend/src/components/ui/sonner.tsx` exists and exports `Toaster`
  - `frontend/src/app/layout.tsx` contains `import { Toaster } from '@/components/ui/sonner'`
  - `frontend/src/app/layout.tsx` contains `<Toaster`
  - `frontend/src/app/layout.tsx` contains `richColors`
  - `frontend/src/app/layout.tsx` contains `position="bottom-right"`
</acceptance_criteria>
<verification>
  - grep "Toaster" frontend/src/app/layout.tsx
  - grep "sonner" frontend/src/components/ui/sonner.tsx
</verification>
</task>

<task id="2" type="execute">
<title>Create Spinner component</title>
<files>
  - frontend/src/components/ui/spinner.tsx (CREATE)
</files>
<read_first>
  - frontend/src/components/ui/button.tsx
</read_first>
<action>
Create `frontend/src/components/ui/spinner.tsx`:

```tsx
import { Loader2 } from 'lucide-react';
import { cn } from '@/lib/utils';

export function Spinner({ size = 16, className }: { size?: number; className?: string }) {
  return <Loader2 size={size} className={cn('animate-spin', className)} />;
}
```

This uses the `Loader2` icon from lucide-react (already installed) with a CSS `animate-spin` class (provided by Tailwind).
</action>
<acceptance_criteria>
  - File `frontend/src/components/ui/spinner.tsx` exists
  - Contains `export function Spinner`
  - Contains `Loader2` import from `lucide-react`
  - Contains `animate-spin`
</acceptance_criteria>
</task>

<task id="3" type="execute">
<title>Create reusable EmptyState component</title>
<files>
  - frontend/src/components/ui/empty-state.tsx (CREATE)
</files>
<read_first>
  - frontend/src/app/globals.css
</read_first>
<action>
Create `frontend/src/components/ui/empty-state.tsx`:

```tsx
import React from 'react';
import Link from 'next/link';

interface EmptyStateProps {
  icon: React.ReactNode;
  title: string;
  description: string;
  actionLabel?: string;
  actionHref?: string;
}

export function EmptyState({ icon, title, description, actionLabel, actionHref }: EmptyStateProps) {
  return (
    <div className="card px-6 py-16 text-center flex flex-col items-center gap-4">
      <div style={{
        width: 72, height: 72, borderRadius: '50%',
        background: 'var(--neutral-50)', border: '1px solid var(--neutral-100)',
        display: 'flex', alignItems: 'center', justifyContent: 'center',
        color: 'var(--neutral-300)'
      }}>
        {icon}
      </div>
      <div>
        <div style={{ fontSize: 18, fontWeight: 700, color: 'var(--neutral-700)', marginBottom: 6 }}>{title}</div>
        <div style={{ fontSize: 14, color: 'var(--neutral-400)', lineHeight: 1.5 }}>{description}</div>
      </div>
      {actionLabel && actionHref && (
        <Link href={actionHref} className="btn btn-primary" style={{ marginTop: 8 }}>
          {actionLabel}
        </Link>
      )}
    </div>
  );
}
```
</action>
<acceptance_criteria>
  - File `frontend/src/components/ui/empty-state.tsx` exists
  - Contains `export function EmptyState`
  - Contains `actionLabel` and `actionHref` props
  - Uses `Link` from `next/link` for the CTA button
</acceptance_criteria>
</task>

<task id="4" type="execute">
<title>Wire toast + spinner into shop listing page</title>
<files>
  - frontend/src/app/(shop)/shop/page.tsx (MODIFY)
</files>
<read_first>
  - frontend/src/app/(shop)/shop/page.tsx
  - frontend/src/components/ui/spinner.tsx
  - frontend/src/components/ui/empty-state.tsx
</read_first>
<action>
In `frontend/src/app/(shop)/shop/page.tsx`:

1. Add imports:
   ```tsx
   import { toast } from 'sonner';
   import { Spinner } from '@/components/ui/spinner';
   import { EmptyState } from '@/components/ui/empty-state';
   import { SearchX } from 'lucide-react';
   ```

2. Replace `addToCartMutation` success/error handlers:
   - `onSuccess`: Replace `alert("Đã thêm vào giỏ hàng!")` with `toast.success("Đã thêm vào giỏ hàng!")`
   - `onError`: Replace `alert(err.response?.data?.detail || "Lỗi khi thêm giỏ hàng")` with `toast.error(err.response?.data?.detail || "Lỗi khi thêm giỏ hàng")`

3. Replace guest cart alert:
   - In `handleAddToCart`, replace `alert("Đã thêm vào giỏ hàng tạm thời...")` with `toast.success("Đã thêm vào giỏ hàng!")`

4. Update `ProductCard` button to show spinner:
   - Change the button content from `<ShoppingCart size={14} /> Thêm giỏ` to:
   ```tsx
   {isPending ? <Spinner size={14} /> : <ShoppingCart size={14} />} {isPending ? "Đang thêm..." : "Thêm giỏ"}
   ```

5. Add empty search results state after the product grid (inside the `data.items` rendering area):
   After `data.items.map(...)` closing div, before pagination, add:
   ```tsx
   {data.items.length === 0 && (
     <div className="col-span-full">
       <EmptyState
         icon={<SearchX size={32} />}
         title="Không tìm thấy sản phẩm"
         description={search ? `Không có kết quả cho "${search}". Hãy thử từ khóa khác.` : "Không có sản phẩm nào phù hợp với bộ lọc đã chọn."}
         actionLabel="Xem tất cả sản phẩm"
         actionHref="/shop"
       />
     </div>
   )}
   ```
</action>
<acceptance_criteria>
  - `shop/page.tsx` contains `import { toast } from 'sonner'`
  - `shop/page.tsx` contains `toast.success` (at least 1 occurrence)
  - `shop/page.tsx` contains `toast.error` (at least 1 occurrence)
  - `shop/page.tsx` does NOT contain `alert("Đã thêm` 
  - `shop/page.tsx` contains `Spinner`
  - `shop/page.tsx` contains `EmptyState`
  - `shop/page.tsx` contains `SearchX`
  - `shop/page.tsx` contains `Không tìm thấy sản phẩm`
</acceptance_criteria>
</task>

<task id="5" type="execute">
<title>Wire toast + spinner into product detail page</title>
<files>
  - frontend/src/app/(shop)/products/[slug]/page.tsx (MODIFY)
</files>
<read_first>
  - frontend/src/app/(shop)/products/[slug]/page.tsx
  - frontend/src/components/ui/spinner.tsx
</read_first>
<action>
In `frontend/src/app/(shop)/products/[slug]/page.tsx`:

1. Add imports:
   ```tsx
   import { toast } from 'sonner';
   import { Spinner } from '@/components/ui/spinner';
   ```

2. Update `addToCartMutation`:
   - `onSuccess`: Replace `alert("Đã thêm vào giỏ hàng thành công!")` with `toast.success("Đã thêm vào giỏ hàng!")`
   - `onError`: Replace `alert(err.response?.data?.detail || "Lỗi khi thêm vào giỏ hàng")` with `toast.error(err.response?.data?.detail || "Lỗi khi thêm vào giỏ hàng")`

3. Update guest cart:
   - In `handleAddToCart`, replace `alert("Đã thêm vào giỏ hàng tạm thời...")` with `toast.success("Đã thêm vào giỏ hàng!")`

4. Update `handleBuyNow` error handler:
   - Replace `alert(e.response?.data?.detail || "Lỗi khi xử lý...")` with `toast.error(e.response?.data?.detail || "Lỗi khi xử lý. Vui lòng thử lại.")`

5. Update add-to-cart button (line ~313-320):
   Replace the button content:
   ```tsx
   {addToCartMutation.isPending ? <Spinner size={20} /> : <ShoppingCart size={20} />} {addToCartMutation.isPending ? "Đang thêm..." : "Thêm vào giỏ hàng"}
   ```

6. Update buy-now button (line ~322-329):
   Replace the button content:
   ```tsx
   {buyNowLoading ? <><Spinner size={18} /> Đang xử lý...</> : "Mua ngay"}
   ```
</action>
<acceptance_criteria>
  - `products/[slug]/page.tsx` contains `import { toast } from 'sonner'`
  - `products/[slug]/page.tsx` contains `toast.success`
  - `products/[slug]/page.tsx` contains `toast.error`
  - `products/[slug]/page.tsx` does NOT contain `alert("Đã thêm`
  - `products/[slug]/page.tsx` contains `Spinner`
</acceptance_criteria>
</task>

<task id="6" type="execute">
<title>Enhance empty states for cart and orders pages</title>
<files>
  - frontend/src/app/(shop)/cart/page.tsx (MODIFY)
  - frontend/src/app/(shop)/orders/page.tsx (MODIFY)
</files>
<read_first>
  - frontend/src/app/(shop)/cart/page.tsx
  - frontend/src/app/(shop)/orders/page.tsx
  - frontend/src/components/ui/empty-state.tsx
</read_first>
<action>
**Cart page** (`frontend/src/app/(shop)/cart/page.tsx`):

1. Add import:
   ```tsx
   import { EmptyState } from '@/components/ui/empty-state';
   import { ShoppingBag } from 'lucide-react';
   ```

2. Replace the empty cart block (lines ~199-203) from:
   ```tsx
   <div className="text-center py-20 px-6 bg-white rounded-3xl border border-dashed border-neutral-200">
     <p style={{ color: 'var(--neutral-500)', marginBottom: 24 }}>Giỏ hàng của bạn đang trống.</p>
     <Link href="/shop" className="btn btn-outline">Tiếp tục mua sắm</Link>
   </div>
   ```
   With:
   ```tsx
   <EmptyState
     icon={<ShoppingBag size={32} />}
     title="Giỏ hàng trống"
     description="Bạn chưa thêm sản phẩm nào vào giỏ hàng. Khám phá cửa hàng ngay!"
     actionLabel="Tiếp tục mua sắm"
     actionHref="/shop"
   />
   ```

**Orders page** (`frontend/src/app/(shop)/orders/page.tsx`):
- The existing empty state is already decent (uses `<Package>` icon). Keep it but replace with the EmptyState component for consistency:

1. Add import:
   ```tsx
   import { EmptyState } from '@/components/ui/empty-state';
   ```

2. Replace the empty orders block (lines ~45-52) with:
   ```tsx
   {orders?.length === 0 && (
     <EmptyState
       icon={<Package size={32} />}
       title="Chưa có đơn hàng nào"
       description="Hãy khám phá cửa hàng và đặt hàng cho bé pet nhé!"
       actionLabel="Mua sắm ngay"
       actionHref="/shop"
     />
   )}
   ```
</action>
<acceptance_criteria>
  - `cart/page.tsx` contains `EmptyState`
  - `cart/page.tsx` contains `ShoppingBag`
  - `cart/page.tsx` contains `Giỏ hàng trống`
  - `orders/page.tsx` contains `EmptyState`
  - `orders/page.tsx` contains `Chưa có đơn hàng nào`
</acceptance_criteria>
</task>

</tasks>

<verification>
1. Run `npm run build` in `frontend/` — no TypeScript errors
2. Visit `/shop` → add to cart → toast appears (no alert)
3. Visit `/shop?q=nonexistent` → empty search state with icon displayed
4. Visit `/cart` with empty cart → EmptyState with "Giỏ hàng trống" + CTA
5. Visit `/orders` with no orders → EmptyState with "Chưa có đơn hàng nào" + CTA
6. Visit `/products/[slug]` → add to cart → spinner on button + toast
</verification>

<success_criteria>
- All `alert()` calls in shop, product detail pages replaced with `toast()`
- Spinner icon visible on add-to-cart, buy-now buttons during mutation
- Empty cart shows EmptyState component with icon + CTA
- Empty orders shows EmptyState component with icon + CTA
- No-results search shows EmptyState with helpful message
</success_criteria>

<must_haves>
<truths>
  - Sonner Toaster component mounted at app root in layout.tsx
  - Toast uses richColors and position="bottom-right"
  - Spinner uses Loader2 from lucide-react with animate-spin
  - EmptyState component is reusable across pages
  - No alert() calls remain in shop or product detail pages
</truths>
</must_haves>
