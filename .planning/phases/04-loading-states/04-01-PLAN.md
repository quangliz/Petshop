---
phase: 04-loading-states
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/ui/skeleton.tsx
  - frontend/src/components/skeletons/ProductCardSkeleton.tsx
  - frontend/src/components/skeletons/ProductDetailSkeleton.tsx
  - frontend/src/components/skeletons/CartRowSkeleton.tsx
  - frontend/src/components/skeletons/OrderRowSkeleton.tsx
autonomous: true
requirements: [UX-01, UX-02, UX-03, UX-04]
must_haves:
  truths:
    - "Skeleton primitive exists at src/components/ui/skeleton.tsx and re-exports a Skeleton React component"
    - "Four page-specific skeleton components exist under src/components/skeletons/ and import the Skeleton primitive"
    - "Skeleton wrappers mirror the real page DOM structure (grid for shop, two-column for detail, row for cart, card for orders)"
    - "Project type-checks cleanly (npm run build) after the new components are added"
  artifacts:
    - path: "frontend/src/components/ui/skeleton.tsx"
      provides: "Base Skeleton primitive (animate-pulse rounded-md bg-accent)"
      contains: "export { Skeleton }"
    - path: "frontend/src/components/skeletons/ProductCardSkeleton.tsx"
      provides: "Skeleton card mirroring shop product card (image + brand + name + price)"
      contains: "export function ProductCardSkeleton"
    - path: "frontend/src/components/skeletons/ProductDetailSkeleton.tsx"
      provides: "Two-column skeleton mirroring product detail page"
      contains: "export function ProductDetailSkeleton"
    - path: "frontend/src/components/skeletons/CartRowSkeleton.tsx"
      provides: "Skeleton row mirroring a cart item row"
      contains: "export function CartRowSkeleton"
    - path: "frontend/src/components/skeletons/OrderRowSkeleton.tsx"
      provides: "Skeleton row mirroring an order history card"
      contains: "export function OrderRowSkeleton"
  key_links:
    - from: "src/components/skeletons/*.tsx"
      to: "src/components/ui/skeleton.tsx"
      via: "import { Skeleton } from \"@/components/ui/skeleton\""
      pattern: "from \"@/components/ui/skeleton\""
---

<objective>
Install the shadcn Skeleton primitive and build four reusable, page-specific skeleton components that downstream plans (02, 03) will drop into existing isLoading guards.

Purpose: Establish the visual building blocks needed to satisfy UX-01..UX-04. Without these components, the page-level guard replacements cannot be done.
Output: 1 primitive + 4 skeleton components, all type-checking cleanly.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-loading-states/04-RESEARCH.md
@.planning/phases/04-loading-states/04-PATTERNS.md
@frontend/src/components/ui/card.tsx
@frontend/src/lib/utils.ts

<interfaces>
<!-- Skeleton primitive contract (from shadcn). Downstream skeleton components consume this. -->
```tsx
// src/components/ui/skeleton.tsx
import { cn } from "@/lib/utils"

function Skeleton({ className, ...props }: React.ComponentProps<"div">) {
  return (
    <div
      data-slot="skeleton"
      className={cn("animate-pulse rounded-md bg-accent", className)}
      {...props}
    />
  )
}

export { Skeleton }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install shadcn Skeleton primitive</name>
  <files>frontend/src/components/ui/skeleton.tsx</files>
  <read_first>
    - frontend/src/components/ui/card.tsx (reference shadcn primitive structure already in repo)
    - frontend/src/lib/utils.ts (confirm cn() helper export)
    - frontend/components.json (confirm shadcn config so the CLI works)
  </read_first>
  <action>
    From the `frontend/` directory, run: `npx shadcn@latest add skeleton`
    This creates `frontend/src/components/ui/skeleton.tsx` with the exact contents shown in <interfaces> above.

    If the CLI fails (offline / network), fall back to writing the file manually with these EXACT contents:

    ```tsx
    import { cn } from "@/lib/utils"

    function Skeleton({ className, ...props }: React.ComponentProps<"div">) {
      return (
        <div
          data-slot="skeleton"
          className={cn("animate-pulse rounded-md bg-accent", className)}
          {...props}
        />
      )
    }

    export { Skeleton }
    ```

    Do NOT hand-roll a different shimmer animation. Do NOT replace `bg-accent` with a hard-coded colour — it maps to the project's design token.
  </action>
  <verify>
    <automated>cd frontend && test -f src/components/ui/skeleton.tsx && grep -q "export { Skeleton }" src/components/ui/skeleton.tsx && grep -q "animate-pulse" src/components/ui/skeleton.tsx && grep -q "bg-accent" src/components/ui/skeleton.tsx</automated>
  </verify>
  <acceptance_criteria>
    - File `frontend/src/components/ui/skeleton.tsx` exists
    - File contains literal string `export { Skeleton }`
    - File contains literal string `animate-pulse rounded-md bg-accent`
    - File imports `cn` from `@/lib/utils`
    - `cd frontend && npx tsc --noEmit` exits 0 (or `npm run build` exits 0)
  </acceptance_criteria>
  <done>Skeleton primitive available for import as `@/components/ui/skeleton`.</done>
</task>

<task type="auto">
  <name>Task 2: Build four page-specific skeleton components</name>
  <files>
    frontend/src/components/skeletons/ProductCardSkeleton.tsx,
    frontend/src/components/skeletons/ProductDetailSkeleton.tsx,
    frontend/src/components/skeletons/CartRowSkeleton.tsx,
    frontend/src/components/skeletons/OrderRowSkeleton.tsx
  </files>
  <read_first>
    - frontend/src/components/ui/skeleton.tsx (just created — confirm import path)
    - frontend/src/app/(shop)/shop/page.tsx (real product card grid layout; lines around 354)
    - frontend/src/app/(shop)/products/[slug]/page.tsx (real detail two-column layout; lines around 151-162)
    - frontend/src/app/(shop)/cart/page.tsx (real cart row layout; lines around 208-217)
    - frontend/src/app/(shop)/orders/page.tsx (real order card layout; lines around 54-58)
    - .planning/phases/04-loading-states/04-PATTERNS.md (skeleton component code blocks)
  </read_first>
  <action>
    Create the directory `frontend/src/components/skeletons/` if it does not exist, then write FOUR files with these EXACT contents:

    **File 1: `frontend/src/components/skeletons/ProductCardSkeleton.tsx`**
    ```tsx
    import { Skeleton } from "@/components/ui/skeleton";

    export function ProductCardSkeleton() {
      return (
        <div className="rounded-xl overflow-hidden">
          <Skeleton className="aspect-square w-full" />
          <div className="p-3 flex flex-col gap-2">
            <Skeleton className="h-3 w-16" />
            <Skeleton className="h-4 w-full" />
            <Skeleton className="h-4 w-3/4" />
            <Skeleton className="h-5 w-24 mt-1" />
          </div>
        </div>
      );
    }
    ```

    **File 2: `frontend/src/components/skeletons/ProductDetailSkeleton.tsx`**
    ```tsx
    import { Skeleton } from "@/components/ui/skeleton";

    export function ProductDetailSkeleton() {
      return (
        <div className="max-w-[1200px] mx-auto px-4 md:px-6 py-6 md:py-8">
          <div className="grid grid-cols-1 md:grid-cols-2 gap-8 lg:gap-16 items-start">
            <Skeleton className="aspect-square w-full rounded-xl" />
            <div className="flex flex-col gap-4">
              <Skeleton className="h-4 w-24" />
              <Skeleton className="h-10 w-full" />
              <Skeleton className="h-10 w-2/3" />
              <Skeleton className="h-16 w-full rounded-xl" />
              <Skeleton className="h-10 w-full" />
            </div>
          </div>
        </div>
      );
    }
    ```

    **File 3: `frontend/src/components/skeletons/CartRowSkeleton.tsx`**
    ```tsx
    import { Skeleton } from "@/components/ui/skeleton";

    export function CartRowSkeleton() {
      return (
        <div className="flex items-center gap-4 p-5 rounded-xl border border-neutral-100">
          <Skeleton className="h-4 w-4 rounded flex-shrink-0" />
          <Skeleton className="h-[100px] w-[100px] rounded-xl flex-shrink-0" />
          <div className="flex-1 flex flex-col gap-2">
            <Skeleton className="h-4 w-48" />
            <Skeleton className="h-3 w-24" />
          </div>
          <Skeleton className="h-8 w-28" />
        </div>
      );
    }
    ```

    **File 4: `frontend/src/components/skeletons/OrderRowSkeleton.tsx`**
    ```tsx
    import { Skeleton } from "@/components/ui/skeleton";

    export function OrderRowSkeleton() {
      return <Skeleton className="h-24 w-full rounded-xl" />;
    }
    ```

    All four files use NAMED exports (matches shadcn convention). Do NOT add `"use client"` — these are pure presentational components with no hooks. Do NOT import `React` unless TS strict-jsx requires it (the project does not require it; see card.tsx style).
  </action>
  <verify>
    <automated>cd frontend && test -f src/components/skeletons/ProductCardSkeleton.tsx && test -f src/components/skeletons/ProductDetailSkeleton.tsx && test -f src/components/skeletons/CartRowSkeleton.tsx && test -f src/components/skeletons/OrderRowSkeleton.tsx && grep -l "from \"@/components/ui/skeleton\"" src/components/skeletons/*.tsx | wc -l | grep -q '^4$' && npm run lint && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - All four files exist under `frontend/src/components/skeletons/`
    - Each file contains `import { Skeleton } from "@/components/ui/skeleton";`
    - `ProductCardSkeleton.tsx` contains literal string `export function ProductCardSkeleton`
    - `ProductDetailSkeleton.tsx` contains literal strings `grid-cols-1 md:grid-cols-2` and `aspect-square`
    - `CartRowSkeleton.tsx` contains literal string `h-[100px] w-[100px]`
    - `OrderRowSkeleton.tsx` contains literal string `h-24 w-full rounded-xl`
    - `cd frontend && npm run lint && npm run build` exits 0
  </acceptance_criteria>
  <done>Four skeleton components compiled and ready for import by Plan 02 and Plan 03.</done>
</task>

</tasks>

<verification>
- `cd frontend && npm run lint && npm run build` exits 0
- All five files exist and export named symbols
- Each skeleton component imports from `@/components/ui/skeleton`
</verification>

<success_criteria>
- Skeleton primitive exists at the canonical shadcn path
- Four page-specific skeleton components exist with the exact DOM shapes prescribed by 04-PATTERNS.md
- TypeScript build is green; no runtime change to any existing page yet
</success_criteria>

<output>
After completion, create `.planning/phases/04-loading-states/04-01-SUMMARY.md`
</output>
