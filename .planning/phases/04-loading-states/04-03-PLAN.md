---
phase: 04-loading-states
plan: 03
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - frontend/src/app/(shop)/cart/page.tsx
  - frontend/src/app/(shop)/orders/page.tsx
autonomous: true
requirements: [UX-03, UX-04]
must_haves:
  truths:
    - "Visiting /cart shows three skeleton item rows before cart data arrives"
    - "Visiting /orders shows three skeleton order cards before order history arrives"
    - "The text strings 'Dang tai gio hang...' and 'Dang tai don hang...' (Vietnamese loading text) no longer appear in their respective files"
    - "Skeleton wrappers use the same Tailwind container classes as the real layouts so there is no layout shift on resolve"
  artifacts:
    - path: "frontend/src/app/(shop)/cart/page.tsx"
      provides: "Cart page with skeleton-row loading state"
      contains: "CartRowSkeleton"
    - path: "frontend/src/app/(shop)/orders/page.tsx"
      provides: "Orders page with skeleton-row loading state"
      contains: "OrderRowSkeleton"
  key_links:
    - from: "src/app/(shop)/cart/page.tsx"
      to: "src/components/skeletons/CartRowSkeleton.tsx"
      via: "named import"
      pattern: "from \"@/components/skeletons/CartRowSkeleton\""
    - from: "src/app/(shop)/orders/page.tsx"
      to: "src/components/skeletons/OrderRowSkeleton.tsx"
      via: "named import"
      pattern: "from \"@/components/skeletons/OrderRowSkeleton\""
---

<objective>
Replace the text-based isLoading guards in the cart page and the orders history page with skeleton row layouts. Satisfies UX-03 and UX-04.

Purpose: Cart and orders both render lists of "row" items. While that list is loading, users see structured placeholder rows with the same dimensions as real rows.
Output: Two modified page files, each importing one skeleton component and rendering 3 instances inside the existing isLoading guard.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-loading-states/04-RESEARCH.md
@.planning/phases/04-loading-states/04-PATTERNS.md
@frontend/src/app/(shop)/cart/page.tsx
@frontend/src/app/(shop)/orders/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace cart isLoading guard with skeleton rows (UX-03)</name>
  <files>frontend/src/app/(shop)/cart/page.tsx</files>
  <read_first>
    - frontend/src/app/(shop)/cart/page.tsx (current isLoading guard sits at approx line 136 inside AuthCartPage; the real container at approx line 176 uses max-w-[1200px] mx-auto px-4 md:px-6 py-6)
    - frontend/src/components/skeletons/CartRowSkeleton.tsx (created in Plan 04-01)
    - .planning/phases/04-loading-states/04-PATTERNS.md (UX-03 replacement block)
  </read_first>
  <action>
    Step 1. Add this import near the top of `frontend/src/app/(shop)/cart/page.tsx`, alongside other component imports:

    ```
    import { CartRowSkeleton } from "@/components/skeletons/CartRowSkeleton";
    ```

    Step 2. Locate the existing isLoading guard at approximately line 136 inside the `AuthCartPage` component. The exact line currently is:

    ```
    if (isLoading) return <div style={{ padding: 100, textAlign: 'center', color: 'var(--neutral-500)' }}>Dang tai gio hang...</div>;
    ```

    (where `Dang tai gio hang...` is the Vietnamese loading text — verify the exact string in the source file.)

    Step 3. Replace that one-line guard ENTIRELY with this exact block (3 skeleton rows; container matches real CartLayout container at approx line 176):

    ```
    if (isLoading) return (
      <div className="max-w-[1200px] mx-auto px-4 md:px-6 py-6 md:py-8">
        <div className="flex flex-col gap-4">
          {[...Array(3)].map((_, i) => <CartRowSkeleton key={i} />)}
        </div>
      </div>
    );
    ```

    Do NOT change anything outside the isLoading guard. Do NOT change the useQuery hook. Use `isLoading` (NOT `isFetching`) — see RESEARCH.md Pitfall 2.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; grep -q 'from "@/components/skeletons/CartRowSkeleton"' "src/app/(shop)/cart/page.tsx" &amp;&amp; grep -q "CartRowSkeleton key={i}" "src/app/(shop)/cart/page.tsx" &amp;&amp; ! grep -q "tải giỏ hàng" "src/app/(shop)/cart/page.tsx" &amp;&amp; grep -F -q '[...Array(3)]' "src/app/(shop)/cart/page.tsx" &amp;&amp; npm run lint && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - File contains literal `from "@/components/skeletons/CartRowSkeleton"`
    - File contains literal `<CartRowSkeleton key={i} />`
    - File contains literal `[...Array(3)]`
    - File contains literal container class `max-w-[1200px] mx-auto px-4 md:px-6 py-6 md:py-8`
    - File does NOT contain the old Vietnamese loading text "tải giỏ hàng"
    - `cd frontend && npm run lint && npm run build` exits 0
  </acceptance_criteria>
  <done>Cart page renders 3 skeleton item rows while cart data is loading.</done>
</task>

<task type="auto">
  <name>Task 2: Replace orders isLoading guard with skeleton rows (UX-04)</name>
  <files>frontend/src/app/(shop)/orders/page.tsx</files>
  <read_first>
    - frontend/src/app/(shop)/orders/page.tsx (current isLoading guard sits at approx line 26; the real container at approx line 29-30 uses max-w-4xl mx-auto px-4 sm:px-6 py-6 sm:py-8)
    - frontend/src/components/skeletons/OrderRowSkeleton.tsx (created in Plan 04-01)
    - .planning/phases/04-loading-states/04-PATTERNS.md (UX-04 replacement block)
  </read_first>
  <action>
    Step 1. Add this import near the top of `frontend/src/app/(shop)/orders/page.tsx`, alongside other component imports:

    ```
    import { OrderRowSkeleton } from "@/components/skeletons/OrderRowSkeleton";
    ```

    Step 2. Locate the existing isLoading guard at approximately line 26. The exact line currently is:

    ```
    if (isLoading) return <div className="py-24 text-center" style={{ color: 'var(--neutral-500)' }}>Dang tai don hang...</div>;
    ```

    (where `Dang tai don hang...` is the Vietnamese loading text — verify the exact string in the source file.)

    Step 3. Replace that one-line guard ENTIRELY with this exact block (3 skeleton rows; container matches the real shell at approx line 29):

    ```
    if (isLoading) return (
      <div className="max-w-4xl mx-auto px-4 sm:px-6 py-6 sm:py-8">
        <div className="flex flex-col gap-3 sm:gap-4">
          {[...Array(3)].map((_, i) => <OrderRowSkeleton key={i} />)}
        </div>
      </div>
    );
    ```

    Note: the breadcrumb and `<h1>` at lines 31-37 sit OUTSIDE/below the guard — they render only when data has loaded (current behaviour). Do not move them.

    Do NOT change anything outside the isLoading guard. Do NOT change the useQuery hook. Use `isLoading` (NOT `isFetching`).
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; grep -q 'from "@/components/skeletons/OrderRowSkeleton"' "src/app/(shop)/orders/page.tsx" &amp;&amp; grep -q "OrderRowSkeleton key={i}" "src/app/(shop)/orders/page.tsx" &amp;&amp; ! grep -q "tải đơn hàng" "src/app/(shop)/orders/page.tsx" &amp;&amp; grep -F -q '[...Array(3)]' "src/app/(shop)/orders/page.tsx" &amp;&amp; grep -q "max-w-4xl mx-auto px-4 sm:px-6 py-6 sm:py-8" "src/app/(shop)/orders/page.tsx" &amp;&amp; npm run lint && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - File contains literal `from "@/components/skeletons/OrderRowSkeleton"`
    - File contains literal `<OrderRowSkeleton key={i} />`
    - File contains literal `[...Array(3)]`
    - File contains literal container class `max-w-4xl mx-auto px-4 sm:px-6 py-6 sm:py-8`
    - File does NOT contain the old Vietnamese loading text "tải đơn hàng"
    - `cd frontend && npm run lint && npm run build` exits 0
  </acceptance_criteria>
  <done>Orders page renders 3 skeleton order cards while orders data is loading.</done>
</task>

</tasks>

<verification>
- `cd frontend && npm run lint && npm run build` exits 0 after both tasks
- Both pages compile and import named skeleton components
- Old "tải giỏ hàng" / "tải đơn hàng" text guards are gone from their files
- Manual visual check (DevTools network throttle Slow 3G): visit `/cart` while logged in → 3 skeleton rows; visit `/orders` while logged in → 3 skeleton order cards
</verification>

<success_criteria>
- UX-03: Cart page shows 3 skeleton rows before cart data arrives
- UX-04: Orders page shows 3 skeleton rows before order history arrives
- No layout jump when real data replaces skeletons (containers match)
</success_criteria>

<output>
After completion, create `.planning/phases/04-loading-states/04-03-SUMMARY.md`
</output>
