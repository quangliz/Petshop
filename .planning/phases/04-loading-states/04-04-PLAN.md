---
phase: 04-loading-states
plan: 04
type: execute
wave: 3
depends_on: [04-03]
autonomous: true
requirements: []
must_haves:
  truths:
    - "Skeletons are visible because bg-muted resolves to a valid CSS variable"
  artifacts:
    - path: "frontend/src/app/globals.css"
      provides: "Defines --muted variable for Shadcn components"
---

<objective>
Close UAT gaps by ensuring the Shadcn Skeleton primitive has a visible background color.
The `bg-muted` Tailwind class requires the `--muted` CSS variable, which was missing from `globals.css`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/04-loading-states/04-UAT.md
@frontend/src/app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add missing CSS variables for Shadcn</name>
  <files>frontend/src/app/globals.css</files>
  <action>
    Add the following lines under the `/* Shadcn Mappings */` block in `frontend/src/app/globals.css`:
    ```css
    --muted: 38 20% 90%;
    --muted-foreground: 38 20% 40%;
    ```
    This ensures that `bg-muted` resolves to a light gray color, making the `Skeleton` primitive visible.
  </action>
  <verify>
    <automated>grep -q "muted:" frontend/src/app/globals.css</automated>
  </verify>
  <acceptance_criteria>
    - `globals.css` contains `--muted` variable definition
  </acceptance_criteria>
  <done>The Shadcn Skeleton primitive is now visually rendering instead of being transparent.</done>
</task>

</tasks>

<verification>
- Verify that `--muted` is added to `globals.css`.
</verification>

<success_criteria>
- Skeletons are visible to the user.
</success_criteria>
