---
phase: 04-loading-states
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - frontend/src/app/(shop)/shop/page.tsx
  - frontend/src/app/(shop)/products/[slug]/page.tsx
autonomous: true
requirements: [UX-01, UX-02]
must_haves:
  truths:
    - "Visiting /shop shows a grid of skeleton product cards before products arrive (no blank grid, no text spinner)"
    - "Opening a product detail page shows two-column skeleton (image + title + price + button placeholders) before product loads"
    - "The text strings 'Đang tải sản phẩm...' and 'Đang tải...' no longer appear in shop/page.tsx or products/[slug]/page.tsx"
    - "Skeleton wrapper containers use the same Tailwind container classes as the real layouts to prevent layout shift"
  artifacts:
    - path: "frontend/src/app/(shop)/shop/page.tsx"
      provides: "Shop listing page with skeleton-grid loading state"
      contains: "ProductCardSkeleton"
    - path: "frontend/src/app/(shop)/products/[slug]/page.tsx"
      provides: "Product detail page with skeleton loading state"
      contains: "ProductDetailSkeleton"
  key_links:
    - from: "src/app/(shop)/shop/page.tsx"
      to: "src/components/skeletons/ProductCardSkeleton.tsx"
      via: "named import"
      pattern: "import \\{ ProductCardSkeleton \\} from \"@/components/skeletons/ProductCardSkeleton\""
    - from: "src/app/(shop)/products/[slug]/page.tsx"
      to: "src/components/skeletons/ProductDetailSkeleton.tsx"
      via: "named import"
      pattern: "import \\{ ProductDetailSkeleton \\} from \"@/components/skeletons/ProductDetailSkeleton\""
---

<objective>
Replace the text-based isLoading guards in the shop listing page and the product detail page with skeleton layouts that mirror real content. Satisfies UX-01 (UX-02 paired with detail page).

Purpose: Users see structured placeholders matching the final layout the moment the page mounts — no blank grid, no "Đang tải..." text, no layout shift on data arrival.
Output: Two modified page files, each importing and rendering one skeleton component inside its existing isLoading guard.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-loading-states/04-RESEARCH.md
@.planning/phases/04-loading-states/04-PATTERNS.md
@frontend/src/app/(shop)/shop/page.tsx
@frontend/src/app/(shop)/products/[slug]/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace shop listing isLoading guard with skeleton grid (UX-01)</name>
  <files>frontend/src/app/(shop)/shop/page.tsx</files>
  <read_first>
    - frontend/src/app/(shop)/shop/page.tsx (current isLoading guard sits at ~line 274; confirm exact location and surrounding container classes at lines ~278, 338, 354)
    - frontend/src/components/skeletons/ProductCardSkeleton.tsx (the component being imported — created in Plan 04-01)
    - .planning/phases/04-loading-states/04-PATTERNS.md (UX-01 replacement block)
  </read_first>
  <action>
    1. Add this import near the top of `frontend/src/app/(shop)/shop/page.tsx`, alongside other component imports:
       ```tsx
       import { ProductCardSkeleton } from "@/components/skeletons/ProductCardSkeleton";
       ```

    2. Locate the existing isLoading guard (currently at ~line 274):
       ```tsx
       if (isLoading) return <div style={{ padding: 100, textAlign: 'center', color: 'var(--neutral-500)' }}>Đang tải sản phẩm...</div>;
       ```

    3. Replace it ENTIRELY with this exact block (12 skeleton cards, container classes match real layout at line 278):
       ```tsx
       if (isLoading) return (
         <div className="max-w-[1200px] mx-auto px-4 md:px-6 lg:px-8 py-8">
           <div className="grid grid-cols-2 md:grid-cols-3 gap-4 md:gap-6">
             {[...Array(12)].map((_, i) => <ProductCardSkeleton key={i} />)}
           </div>
         </div>
       );
       ```

    Do NOT change anything outside the isLoading guard. Do NOT remove or alter the existing breadcrumb / heading rendering. Do NOT change the `useQuery` hook. Use `isLoading` (NOT `isFetching`) — see RESEARCH.md Pitfall 2.
  </action>
  <verify>
    <automated>cd frontend && grep -q 'import { ProductCardSkeleton } from "@/components/skeletons/ProductCardSkeleton"' "src/app/(shop)/shop/page.tsx" && grep -q "ProductCardSkeleton key={i}" "src/app/(shop)/shop/page.tsx" && ! grep -q "Đang tải sản phẩm" "src/app/(shop)/shop/page.tsx" && grep -q "grid-cols-2 md:grid-cols-3" "src/app/(shop)/shop/page.tsx" && npm run lint && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - File contains literal `import { ProductCardSkeleton } from "@/components/skeletons/ProductCardSkeleton"`
    - File contains literal `<ProductCardSkeleton key={i} />`
    - File contains literal `[...Array(12)]`
    - File does NOT contain literal `Đang tải sản phẩm` (old guard fully removed)
    - File contains the container literal `max-w-[1200px] mx-auto px-4 md:px-6 lg:px-8 py-8`
    - `cd frontend && npm run lint && npm run build` exits 0
  </acceptance_criteria>
  <done>Shop listing page renders 12 skeleton cards in the same grid layout as real products while loading.</done>
</task>

<task type="auto">
  <name>Task 2: Replace product detail isLoading guard with skeleton layout (UX-02)</name>
  <files>frontend/src/app/(shop)/products/[slug]/page.tsx</files>
  <read_first>
    - frontend/src/app/(shop)/products/[slug]/page.tsx (current isLoading guard sits at ~line 143; confirm; the real two-column layout is at ~line 161)
    - frontend/src/components/skeletons/ProductDetailSkeleton.tsx (created in Plan 04-01)
    - .planning/phases/04-loading-states/04-PATTERNS.md (UX-02 replacement block, note about breadcrumb)
  </read_first>
  <action>
    1. Add this import near the top of `frontend/src/app/(shop)/products/[slug]/page.tsx`, alongside other component imports:
       ```tsx
       import { ProductDetailSkeleton } from "@/components/skeletons/ProductDetailSkeleton";
       ```

    2. Locate the existing isLoading guard (currently at ~line 143):
       ```tsx
       if (isLoading) return <div style={{ padding: 100, textAlign: 'center', color: 'var(--neutral-500)' }}>Đang tải...</div>;
       ```

    3. Replace it ENTIRELY with this exact one-liner:
       ```tsx
       if (isLoading) return <ProductDetailSkeleton />;
       ```

    Note from PATTERNS.md: the breadcrumb at ~lines 153-159 uses `product.name` from the query, so it MUST stay BELOW the isLoading guard (i.e., it does not render during loading). The current guard already excludes it correctly — keep that ordering.

    Do NOT change anything outside the isLoading guard. Do NOT change the `useQuery` hook. Use `isLoading` (NOT `isFetching`).
  </action>
  <verify>
    <automated>cd frontend && grep -q 'import { ProductDetailSkeleton } from "@/components/skeletons/ProductDetailSkeleton"' "src/app/(shop)/products/[slug]/page.tsx" && grep -q "if (isLoading) return <ProductDetailSkeleton />" "src/app/(shop)/products/[slug]/page.tsx" && ! grep -E -q 'Đang tải\.\.\.' "src/app/(shop)/products/[slug]/page.tsx" && npm run lint && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - File contains literal `import { ProductDetailSkeleton } from "@/components/skeletons/ProductDetailSkeleton"`
    - File contains literal `if (isLoading) return <ProductDetailSkeleton />`
    - File does NOT contain the old loading text `Đang tải...` (the standalone three-dot Vietnamese loading text)
    - `cd frontend && npm run lint && npm run build` exits 0
  </acceptance_criteria>
  <done>Product detail page renders the two-column skeleton layout while loading.</done>
</task>

</tasks>

<verification>
- `cd frontend && npm run lint && npm run build` exits 0
- Both pages compile and import named skeleton components
- Old "Đang tải..." text guards are gone from both files
- Manual visual check (DevTools network throttle "Slow 3G"): visit `/shop` → see 12 skeleton cards in 2-or-3-column grid; visit any product detail → see two-column skeleton (image + stacked text/buttons)
</verification>

<success_criteria>
- UX-01: Shop listing shows skeleton grid before products arrive — no blank grid, no layout jump
- UX-02: Product detail shows skeleton for image, title, price, description while loading
</success_criteria>

<output>
After completion, create `.planning/phases/04-loading-states/04-02-SUMMARY.md`
</output>
